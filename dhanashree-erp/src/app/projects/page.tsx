import { ModulePage } from "@/components/layout/module-page";

export const metadata = {
  title: "Projects",
};

export default function ProjectsPage() {
  return (
    <ModulePage
      description="Project workspace is the parent context for site logs, transactions, documents and reporting."
      title="Projects"
    />
  );
}
